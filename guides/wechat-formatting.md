# 无来公众号文章排版规范

这套规范用于把文章整理成便于导入微信公众号的正文稿。文章正文仍以 `articles/` 根目录下的日期短名 Markdown 文件为唯一内容来源；运行 `python3 scripts/export_wechat.py` 后，会为每篇已发布文章生成同名 `-wechat.md` 和 `-wechat.html` 文件，也会按时间更新 `articles/README.md` 索引。

## 视觉设计

整体延续无来配图的青松绿与暖象牙配色，留白克制，避免传统宗教海报式装饰。

| 用途 | 样式 |
| --- | --- |
| 正文 | 16px，行高 1.9，正文色 `#354039`，段后 20px |
| 小标题 | 19px，深松绿 `#18372F`，左侧灰苔绿细线 |
| 强调文字 | 深青松绿 `#315B49`，加粗；关键句另加浅苔绿底 `#EFF2EA` |
| 温暖点缀 | 旧铜色 `#8C724C`，只用于小标记，不大面积铺色 |
| 页面底色 | 白色；图片保持原比例、正文宽度自适应 |

核心句和关键词应服务于文章论述，不做满篇加粗。长文通常选 2–4 处重点；同一篇文章不重复用同一种强调句式。

## 微信兼容约束

公众号草稿接口的正文 `content` 字段使用 HTML 内容，详见[微信公众平台新增草稿接口](https://developers.weixin.qq.com/doc/subscription/api/draftbox/draftmanage/api_draft_add.html)。官方接口文档没有承诺所有粘贴样式都会原样保留；公众号编辑器还会处理粘贴内容。第三方公众号排版工具也基于实测建议将 CSS 写成内联样式，并避免依赖 `<style>` 块和 `class`（例如 [MDInline 项目说明](https://github.com/din4e/MDInline)）。因此本项目只使用简单段落、强调、链接和图片标签，将字号、颜色、行距、间距写在元素的 `style` 属性中，不依赖外链 CSS、脚本、复杂布局或浏览器专用效果。

Markdown 本身没有字号和颜色语法，所以 `wechat.md` 使用带内联样式的 HTML 片段，适合导入保留原生 HTML 的 Markdown 排版器；`wechat.html` 是同一份正文的可预览、可复制版本。公众号标题、摘要和封面应在编辑器对应字段中分别填写，不会重复混入正文。

生成文件使用网站公开图片地址，复制或导入后应检查图片是否正常显示；如图片未能保留或需要长期稳定展示，请在公众号后台重新上传对应图片，再发布。

## 使用方式

1. **直接复制：**从仓库下载文件名以 `-wechat.html` 结尾的稿件，用浏览器打开，复制页面正文并粘贴到公众号图文正文区。
2. **Markdown 导入：**将文件名以 `-wechat.md` 结尾的稿件导入支持原生 HTML 的 Markdown 排版器，复制渲染后的富文本；不要把 Markdown 源码当纯文本粘贴到公众号。
3. 在公众号编辑器中单独填写文章标题、摘要和封面，检查正文高亮及三张配图，再保存或发布。

## 发布前检查

- 正文字号保持 16px，手机预览时检查阅读舒适度。
- 核心句高亮与文章原意一致，没有把个人体会包装成普遍教义。
- 图片顺序与正文一致，封面仍单独填写。
- 在微信公众号后台粘贴后检查字号、颜色、段距和图片；编辑器处理结果以后台预览为准。
- `*-wechat.md` 和 `*-wechat.html` 是发布辅助文件，不作为网站文章页面发布。
